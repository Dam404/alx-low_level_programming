#include <stdio.h>
/**
*main - The starting point of my program
*
*Return: (0) ALWAYS SUCCESSFUL
*/
int main(void)
{
char alphabet;

for (alphabet = 'a'; alphabet <= 'z'; alphabet++)

{
putchar(alphabet);
}
putchar('\n');

return (0);
}

