#include "main.h"
/**
 *Puts2 - Prints every other character of a string, starting with
*the first character.
*@s: Pointer to the string
*/
void puts2(char *s)
{
int a = 0;

while (s[a] != '\0')
{
_putchar(str[a]);
a = a + 2;
}
_putchar('\n');
}
